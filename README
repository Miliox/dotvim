Installation:
    git clone git://github.com/Miliox/dotvim.git ~/.vim/

Create symlinks:
    ln -s ~/.vim/.vimrc ~/.vimrc

    or (Windows)
    mklink /H %USERPROFILE%/.vimrc %USERPROFILE%/.vim/.vimrc
